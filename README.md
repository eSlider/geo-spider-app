# Geo Spider App

App to collect, accumulate geo location to then send over internet to main server, as soon device is online.

## Features

- Uses glonas, GPS
- Based on MAUI dotnet
- Mobile first
- Starts and runs in background
- Configurable using config.yaml
- Currently only for android
- Build skript for APK
- TDD
